--- Makefile.in	2025-12-22 23:39:02.953282000 +0000
+++ Makefile.in	2025-12-22 23:39:20.336256285 +0000
@@ -5527,6 +5527,13 @@
 		esac ; \
 	done >>$@
 	# Hurr
+	@f=$(srcdir)/`dirname "$(@D)"`/`basename "$@" .Po | sed s/lib[^-]\*-//` ; \
+	for f in "$$f"*; do \
+		case $$f in \
+			*.c | *.S) echo "$$f"': $$(filter-out $$(DIST_SOURCES),$$(SOURCES))' ;; \
+			*) echo '# dummy';; \
+		esac ; \
+	done >$@-t && $(am__mv) $@-t $@
 
 am--depfiles: $(am__depfiles_remade)
 
